import styles from "./styles.module.scss";

const BoxLogin = () => {
  return (
    <div
      className={`${styles.login__container} ${styles["login__container--animation"]} w-[50rem] h-[40rem] bg-[#fff]`}
    >
      BoxLogin
    </div>
  );
};

export default BoxLogin;
